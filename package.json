{
  "name": "yumawebsite",
  "private": true,
  "version": "0.0.0",
  "description": "Build profiles index and profiles from CSV",
  "scripts": {
    "build:index": "node tools/updateIndex.js",
    "build:csv": "node tools/csv2profiles.js"
  }
}
